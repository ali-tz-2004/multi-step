export const errorMessageRequired = (
  message: string = "این فیلد اجباری است"
): string => {
  return message;
};

export const errorMessageValid = (
  message: string = "این فیلد معتبر نمی باشد"
): string => {
  return message;
};
