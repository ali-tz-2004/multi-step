export const errorMessageRequired = (
  message: string = "MessageRequired"
): string => {
  return message;
};

export const errorMessageValid = (message: string = "MessageValid"): string => {
  return message;
};
